package com.cibertec.repository;

// Transaccion repository removed from order-app because Boleta/Transaccion are handled in the separate boleta microservice.
// This placeholder file kept to avoid accidental compilation errors during incremental development — remove entirely if preferred.

// public interface ITransaccionRepository extends JpaRepository<Transaccion, TransaccionId> {

// }